# Актуальный контекст

## Текущий фокус
На момент анализа проект представляет собой полностью функциональное веб-приложение для прототипирования с полноценной системой логирования, включающее:

- Полноценную систему аутентификации (регистрация, логин, session management)
- REST API для работы с данными (CRUD operations)
- SPA интерфейс на Vue.js с админской панелью
- Миграционную систему БД
- Тестовые данные для демонстрации
- **Структурированное логирование** во всех компонентах (API, datastore, main)
- **Логи в файл** с ротацией через Docker volume
- **Настраиваемые уровни** логирования через environment variables

## Недавние изменения (из .git истории)
Проект был создан как образовательный пример и включает:
- Основную структуру MVC
- Базу данных SQLite с миграциями
- Конфигурацию через environment variables
- Статические HTML страницы с Vue.js компонентами

**Недавние обновления (Phase 8):**
- Добавлен модуль `utils/logger.py` с централизованным логированием
- Обновлена конфигурация: `LOG_LEVEL`, `LOG_FILE` параметры
- Интегрирован логгер во все API endpoints и datastore модули
- Docker конфигурация: volume mount для логов, log directory creation
- Обновлена документация (README, Memory Bank)

## Недавние изменения (из .git истории)
Проект был создан как образовательный пример и включает:
- Основную структуру MVC
- Базу данных SQLite с миграциями
- Конфигурацию через environment variables
- Статические HTML страницы с Vue.js компонентами

## Активные решения
- Использование Bottle вместо более тяжелых фреймворков (Django/FastAPI) для простоты
- Отказ от ORM в пользу raw SQL для наглядности
- MD5 хэширование (небезопасно, но понятно для beginners)
- Подписанные cookies вместо JWT для упрощения
- Vue.js 3 Options API для совместимости и простоты

## Вопросы на рассмотрении
Согласно TODO в README.md, возможные улучшения:
1. Вынести auth_methods в отдельный store (сложность: low)
2. Добавить primary key к auth_methods (сложность: low)
3. Транзакции при создании пользователя (сложность: medium)
4. Разделение БД на микросервисы (auth, userdata, things) (сложность: high)
5. Хранение userroles/permissions в cookie или localStorage (сложность: medium)
6. Реализация Google OAuth (сложность: high)

## Важные паттерны
- **Конфигурация через env vars**: Все настройки читаются из среды, что упрощает развертывание
- **Миграции**: JSON-based миграции позволяют эволюционировать схему БД
- **Инъекция зависимостей**: Bottle plugin автоматически предоставляет db connection
- **Компонентный фронтенд**: Vue.js компоненты организованы в отдельной папке

## Контекст разработки
- **Язык**: Python 3.14 (Docker) / 3.8+ (local)
- **Frontend**: Vue.js 3 + Vue Router 4 + Bootstrap 5
- **Database**: PostgreSQL 17 (Docker) / любой PostgreSQL (local)
- **DevOps**: Docker Compose, ngrok, Python venv
- **Контейнеризация**: Dockerfile + docker-compose.yml с healthchecks
- **Логирование**: File-based логи с ротацией через volume mounts
- **Dependencies**:
  - bottle==0.12.19
  - psycopg2-binary==2.9.9

## Что работает
- ✅ Регистрация новых пользователей
- ✅ Логин с созданием сессии (cookie)
- ✅ Разграничение админ/юзер
- ✅ CRUD операции с вещами (things)
- ✅ Миграции базы данных
- ✅ Запуск через Bottle сервер
- ✅ Статические файлы и SPA маршрутизация
- ✅ **Структурированное логирование** (DEBUG/INFO/WARNING/ERROR)
- ✅ **Логи в файл** с персистентностью через Docker volumes
- ✅ **Настраиваемые уровни** логирования через env vars

## Известные ограничения
- MD5 небезопасен для production
- Нет HTTPS (требуется reverse proxy)
- SQLite не подходит для высокой конкуренции
- Cookies читаемы (не шифрованы)
- Нет валидации на frontend (только backend)

## Следующие шаги (приоритетные)
1. **Тестирование**: Проверить логирование в production-подобной среде (Docker)
2. **Документация**: Добавить API documentation (OpenAPI/Swagger)
3. **Безопасность**: Заменить MD5 на более сильное хэширование
4. **UX/UI**: Улучшить forms validation, error handling на фронтенде
5. **Тесты**: Написать unit/integration тесты для API
6. **DevOps**: Оптимизировать Docker image (multi-stage build, smaller size)

## Контакты и ресурсы
- **Репозиторий**: https://github.com/darumor/simple-python-website
- **Original автор**: darumor
- **Учебное назначение**: OTUS курс (GitLab Course Project Work)